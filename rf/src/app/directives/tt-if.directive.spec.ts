import { TtIfDirective } from './tt-if.directive';

describe('TtIfDirective', () => {
  it('should create an instance', () => {
    const directive = new TtIfDirective();
    expect(directive).toBeTruthy();
  });
});
